- error = @song.errors.include?(:title)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :title, :class => 'control-label'
  .controls
    =f.text_field :title, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:title, :class => 'text-error') : pat(:example)
- error = @song.errors.include?(:tempo)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :tempo, :class => 'control-label'
  .controls
    =f.text_field :tempo, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:tempo, :class => 'text-error') : pat(:example)
- error = @song.errors.include?(:account_id)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :account_id, :class => 'control-label'
  .controls
    =f.text_field :account_id, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:account_id, :class => 'text-error') : pat(:example)
- error = @song.errors.include?(:public)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :public, :class => 'control-label'
  .controls
    =f.check_box :public, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:public, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:songs, :index), :class => 'btn btn-default'